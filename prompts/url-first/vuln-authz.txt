Mode: {{ANALYSIS_MODE}}
Target URL: {{WEB_URL}}
Workspace Path: {{REPO_PATH}}
Source Inventory: {{SOURCE_INVENTORY_PATH}}
Harvest Summary: {{HARVEST_SUMMARY}}

Perform authorization vulnerability analysis (horizontal, vertical, context/flow bypass) from URL-first evidence.
Only include externally exploitable findings.

1. Write `deliverables/authz_analysis_deliverable.md`.
2. Save with `save_deliverable` type `AUTHZ_ANALYSIS` and `file_path`.
3. Save queue with `save_deliverable` type `AUTHZ_QUEUE` and
   `content: {"vulnerabilities":[...]}` (empty list if none).

Output: AUTHORIZATION ANALYSIS COMPLETE
