Mode: {{ANALYSIS_MODE}}
Target URL: {{WEB_URL}}
Workspace Path: {{REPO_PATH}}
Source Inventory: {{SOURCE_INVENTORY_PATH}}
Harvest Summary: {{HARVEST_SUMMARY}}

Perform XSS vulnerability analysis (reflected, stored, DOM-based) from URL-first evidence.
Only include externally exploitable findings with source-to-sink rationale.

1. Write `deliverables/xss_analysis_deliverable.md`.
2. Save with `save_deliverable` type `XSS_ANALYSIS` and `file_path`.
3. Save queue with `save_deliverable` type `XSS_QUEUE` and
   `content: {"vulnerabilities":[...]}` (empty list if none).

Output: XSS ANALYSIS COMPLETE
